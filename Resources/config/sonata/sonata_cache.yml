sonata_cache: ~
#    caches:
#        esi:
#            token: an unique security key # a random one is generated by default
#            servers:
#                - varnishadm -T 127.0.0.1:2000 {{ COMMAND }} "{{ EXPRESSION }}"
#
#        ssi:
#            token: an unique security key # a random one is generated by default

#        apc:
#            token:  symfo2TokenAPC   # token used to clear the related cache
#            prefix: overscanAPC     # prefix to ensure there is no clash between instances
#            servers:
#                - { domain: localhost, ip: 127.0.0.1, port: 80}